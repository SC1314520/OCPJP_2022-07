/*
 *  Consumer: 消費型接口：泛型執行什麼類型，就可以使用 accept() 方法消費什麼類型的數據。
 *  至於具體怎麼消費（使用），需要自定義（輸出,計算...)
 */
 
 /*
 *   predicate:
 *   	對某種數據類型進行判斷，結果返回一個 boolean 值。 --> test(T t)
 */
 
 /*
 *   Supplier<T> 接口中僅包含一個無參方法： T get()  --> 生產型接口。
 */
 
 /*
 *	 Function<T,R> 接口用來根據一個類型數據得到另一個類型的數據。 R apply()
 */